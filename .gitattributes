# 换行规范(spec: fixture-line-endings-break-on-windows.md)
#
# 没有这份文件时,Windows 上 git 会按 core.autocrlf 把 checkout 出来的文本
# 改成 CRLF,造成两类持续故障:
#   ① 逐字断言 fixture 正文的用例在 Windows 红、在 CI(ubuntu)绿;
#   ② biome 对每个文件都报格式错,假警淹掉真问题。

# 默认:仓库里一律存 LF,checkout 也用 LF(不随平台变)。
* text=auto eol=lf

# 测试语料:被逐字断言的真实执行器输出。
# 用 eol=lf(而不是 -text):这些样本入库时就是 LF,断言也要求正文无 \r,
# 所以要的是「任何平台 checkout 都拿到 LF」。
#
# ⚠️ 别改成 -text。-text 是「不做转换、原样存」,在 Windows 上跑
# `git add --renormalize` 会把 checkout 时被加上的 CRLF 原样写回仓库
# —— 检视者 2026-09-08 就这么踩过一次,把 152 个 \r 烤进了 blob。
packages/backend/server/test/fixtures/** text eol=lf

# Windows 本机脚本:由 Windows 工具链执行,保留 CRLF 更合适。
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制:不做任何转换,也不做 diff。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
