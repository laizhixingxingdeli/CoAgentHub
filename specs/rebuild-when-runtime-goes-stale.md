# Spec: 运行时陈旧时自动重建,不再依赖人工

> **状态**: v1.0 Landed(L3 通过 2026-08-27,实现 `850b71c7`);
> **v1.1 新增 R7,Ready for Implementation**
> **版本**: 1.1
> **日期**: 2026-08-30

## 现象

每落地一票就必须有人手工 `build + restart`,否则下一票必然撞死锁:
执行器改了源码 → dist 落后 → 结案撞上旧构建里的守卫 → 任务 `failed`。

本轮实测:检视者手工重建 **4 次**,其中 `01a03e60` 就是因为守卫已在源码删除、
dist 未重建而失败。这类失败与实现质量无关,纯粹是构建滞后。

## 现有基础设施(已具备,不要重写)

- `scripts/coagenthub-prod.sh restart --build` —— 带构建的重启,已实现且幂等
- `scripts/coagenthub-watchdog.sh` —— 每 5 分钟巡检,不健康则 restart
- `GET /api/health` —— 返回 `stale` 与 `staleReason`(`build`/`process`/`both`)
- `runtime-status.ts` —— 已在扫源码树并缓存,`buildStale` 判据为
  「最新源码文件 mtime > dist/server.mjs mtime」

## 缺口

1. 看门狗只判「进程活不活」(`health_ok`),**不判构建新不新**
2. 看门狗第 45 行 `"$PROD_SCRIPT" restart` **不带 `--build`**,
   即使触发也修不好陈旧
3. cron 条目**根本没装**(`crontab -l` 无 coagenthub 条目)

## 要做的

### R1 — 看门狗把「构建陈旧」纳入巡检

`coagenthub-watchdog.sh` 的单次检查中,除现有健康判断外,再读
`/api/health` 的 `stale`/`staleReason`。当 `staleReason` 为 `build` 或 `both`
时,视为需要恢复。

⚠️ `staleReason` 为 `process` 时**不触发重建** —— 那是 entry 比进程启动新,
重启即可,不需要重新构建。

### R2 — 因陈旧触发的恢复必须带 `--build`

因 R1 判据触发时调用 `"$PROD_SCRIPT" restart --build`。
⚠️ 原有的「不健康」路径**保持现状**,仍调用不带 `--build` 的 `restart` ——
进程挂掉时重新构建只会拖慢恢复。两条路径分开,不要合并成一条。

### R3 — ⚠️ 必须防重建风暴

构建失败时 dist 不会更新,`stale` 仍为真,下一轮巡检会再次触发 ——
**这会变成每 5 分钟无限重建**。必须有退避:

- 连续失败达到阈值(建议 3 次)后停止自动重建,只记日志
- 一次成功的重建后计数归零
- 状态持久化到文件(看门狗是 `--once` 单次调用,内存态不跨调用)

### R4 — ⚠️ 不得在任务执行期间重建

重建会重启后端,正在跑的任务会失联。重建前必须确认**没有 running/queued 任务**;
有在途任务则跳过本轮,记日志,等下一轮。

### R5 — 安装 cron 条目

`cron-install` 已实现但从未执行。本票需实际安装并验证条目存在。
⚠️ 保持幂等,重复安装不得产生重复条目。

### R7 — 构建必须在停机之前完成(v1.1 新增)

`restart --build` 的停机窗口**覆盖了整个构建过程**,看门狗会在这个窗口里
把服务用**旧产物**拉起来 —— 恰好破坏本 spec 要保证的「运行时不陈旧」。

**顺序(`scripts/coagenthub-prod.sh`):**

```
cmd_restart:  cmd_stop → wait_free(确认端口空) → cmd_start "$@"
cmd_start:    解析 --build → 【在这里才构建,数秒】 → 启动
```

停机从 `cmd_stop` 开始,到构建结束后才重新监听。看门狗每 5 分钟一轮,
在这个窗口里判定 server 不健康 → 调 `coagenthub-prod.sh restart`(**不带
`--build`**)→ 用磁盘上尚未更新的 dist 把服务拉起。

**实证(2026-08-30 13:32,第 4 次复现,这次时间戳是干净的):**

```
/api/health:  startedAt  2026-08-30T05:32:54.068Z
              entryMtime 2026-08-30T05:32:56.243Z   ← 产物比进程启动晚 2.2 秒
              stale: true, staleReason: "process"

脚本输出:     SKIP  server :3001 已被占用 (pid 15698),跳过
              OK    restart : 进程已替换 server 52896→15698
```

`15698` 不是本次 `cmd_start` 启动的 —— 它在 `cmd_start` 到达启动步之前就已监听,
于是走了「端口占用则跳过」的幂等分支;而 `cmd_restart` 的 R3 校验因看门狗
同样写了 PID 文件而未能识破,最终报告「进程已替换」**成功**。

⚠️ 危害是**静默的**:命令返回 0、日志写着替换成功,实际跑的是旧构建。
前三次复现都因此被误判成「重启没生效」而反复重启。本条正是
`feedback_verify_runtime_is_current` 那条纪律存在的原因 —— 但纪律只能事后发现,
不能防止发生。

**要求:**

- `cmd_restart` 中,`--build` 的构建**必须在 `cmd_stop` 之前**完成。
  构建成功后再停机、启动,停机窗口缩到只剩进程切换。
- 构建失败 → **不停机**,原进程继续服务,非零退出。
  (现状是先停机再构建,构建失败就留下一个停机的系统。)
- `cmd_start --build` 单独调用时的行为**逐字不变**(它没有停机窗口问题)。
- ⚠️ 二选一即可,不要都做:上面是首选;若实现者认为需要更强保证,
  可**另加**一把重启锁文件,看门狗在锁存在时跳过本轮并记录日志。
  **但锁不能替代调序** —— 调序消除窗口,锁只是缩小竞争面。

### R6 — ⚠️ 范围限制

不改 `runtime-status.ts` 的 `stale` 计算方式,不改 `coordinationCloseError`,
不动任何结案守卫。本票只动看门狗脚本与 cron 安装。

## 验收要点

- 制造陈旧(`touch` 一个源码文件)后跑 `coagenthub-watchdog.sh --once`,
  验证:检测到 `staleReason=build` → 执行带 `--build` 的 restart →
  再查 `/api/health` 的 `stale` 变为 `false`(贴出前后两次 health 输出)
- 有 running 任务时跑一次,验证**跳过**且记日志(R4)
- `staleReason=process` 时验证**不触发**重建(R1)
- 模拟构建失败,验证连续 3 次后停止重试(R3,贴日志)
- `crontab -l` 能看到条目;重复跑 `cron-install` 条目数不变(R5)
- 测试全绿,贴出用例数;**基线 694**

### 验收要点(v1.1 R7)

- `restart --build` 期间,端口的不可用时长**与构建时长无关**(只剩进程切换)。
  可用「构建人为拖慢」的方式断言:构建耗时增加时,停机窗口不随之增加。
- 构建失败时:**原进程仍在监听**,命令非零退出,不留下停机系统。必测。
- `restart --build` 成功返回后,`/api/health` 的 `stale` 必须为 `false`。
  ⚠️ 这是本条的核心断言 —— 旧实现会返回 0 且报告「进程已替换」,但 `stale: true`。
- `start --build` 单独调用的行为逐字不变(回归)。
- 看门狗既有的 R3 退避、R4 在途保护行为逐字不变。

## 注意

⚠️ 本票**必须下发给执行器**完成。
⚠️ 结案若被守卫拒绝,如实回报拒绝原文,**不要自行重启后端,也不要以 failed 收场**。
⚠️ 做完记得提交。

## 修订记录

- **v1.1(2026-08-30)**:新增 R7。起因:`restart --build` 的停机窗口覆盖整个
  构建过程,看门狗在窗口内用旧产物拉起服务,命令却报告「进程已替换」成功
  (实证 2026-08-30 13:32,`startedAt` 比 `entryMtime` 早 2.2 秒,第 4 次复现)。
  v1.0 让看门狗在陈旧时自动重建,方向正确;漏的是**重建过程本身会制造一次
  可被看门狗误判的停机**,从而把旧产物重新装回去。
