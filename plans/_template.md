# Plan: <feature name>

> 配套 `specs/<feature>.md`。spec 冻结后 `specHash` 作验收锚点；**本文件会变**（回填 taskId、标完成），不要写入 specs/。
> 路径规则：`specs/<name>.md` → `plans/<name>.md`（从 `specRef` 推得）。
> 平台不解析本文件。计划是提示，**task 事实是权威**。

## 元信息

| 字段 | 值 |
|---|---|
| specRef | `specs/<name>.md` |
| specHash | `<冻结后填写；草案阶段可空>` |
| 编制 | 三方 / 两方 |
| 更新 | YYYY-MM-DD |

## 工作项

> **工作项 ≠ task**：工作项 = 要完成的事；task = 一次执行。重派的多个 task 可属同一工作项。
> **未派发工作项不得伪装成 queued task。**

### W1 — <短标题>

| 字段 | 内容 |
|---|---|
| 稳定编号 | W1 |
| 目标 | |
| 范围 | |
| 前置依赖 | 无 / W? |
| 预期产物 | |
| 验收方法 | |
| specRef | `specs/<name>.md` |
| specHash | |
| taskId | _未派发_ |
| 状态 | planned / dispatched / done / blocked |

### W2 — <短标题>

| 字段 | 内容 |
|---|---|
| 稳定编号 | W2 |
| 目标 | |
| 范围 | |
| 前置依赖 | W1 |
| 预期产物 | |
| 验收方法 | |
| specRef | `specs/<name>.md` |
| specHash | |
| taskId | _未派发_ |
| 状态 | planned |

## 依赖图

```
W1 → W2 → …
```

## 诊断（Bug / 修复票；下发修复前填写）

> 根因未定时先派**带停止条件的调查任务**，不把猜测写成执行器必须遵守的事实。

| 字段 | 内容 |
|---|---|
| 现象 | |
| 期望行为 | |
| 复现步骤 | |
| 已观察事实及证据位置 | （路径 / 日志 / taskId / commit） |
| 根因假设 | |
| 验证动作与结果 | |
| **被排除的假设** | （写明曾怀疑什么、如何否决——避免后人重走死路） |
| 建议修复范围 | |
| **不能改变的行为** | |
| 回归场景 | |
| 最终产物验收方式 | |
